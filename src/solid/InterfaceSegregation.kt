package solid

/**
 * Objetivo: Evitar a criação de interfaces grandes e complexas; as interfaces devem ser específicas para as necessidades dos clientes.
 *
 * Exercício:
 *
 * Crie um sistema de notificação com diferentes tipos de notificações (e-mail, SMS e notificação push). Ao invés de ter uma interface genérica Notificacao, crie interfaces separadas para cada tipo de notificação: NotificacaoEmail, NotificacaoSMS, NotificacaoPush. Isso permite que as classes implementem apenas o que realmente precisam.
 */

class InterfaceSegregation {
}